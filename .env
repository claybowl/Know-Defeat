# GCP Cloud SQL Connection Parameters
DB_USER=postgres
DB_PASSWORD=musicman
DB_NAME=tick_data
DB_HOST=127.0.0.1
DB_PORT=5432

# If you're using Cloud SQL Proxy, use localhost
# If connecting directly, use the Cloud SQL instance IP

# The connection name below is used by Cloud Run and other App Engine services
CLOUD_SQL_CONNECTION_NAME=know-defeat-trading:us-central1:trading-db